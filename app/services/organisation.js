import Service from '@ember/service';

export default Service.extend({
  /**
   * The actual instance of `organisation`.
   *
   * @property currentOrganisation
   * @type OrganisationId
   */
  currentOrganisation: null
});
